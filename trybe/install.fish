#!/usr/bin/env fish

abbr -a tdb 'tsh db login --db-user teleport trybe-'
abbr -a tlg 'tsh login --proxy teleport.betrybe.com'
abbr -a tsts 'tsh status'
abbr -a tlp 'telepresence connect --context teleport.betrybe.com-trybe-staging-eks-cluster'
abbr -a tlpq 'telepresence quit'
